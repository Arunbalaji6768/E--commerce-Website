# Copy this file to `.env.local` and fill the values below
# Never commit your real `.env.local`

# Full MongoDB Atlas connection string (SRV)
# IMPORTANT: URL-encode any special characters in your password (e.g., @ -> %40, # -> %23)
MONGODB_URI=mongodb+srv://prarunbalaji853:YOUR_URL_ENCODED_PASSWORD@cluster0.k8zw842.mongodb.net/ecommerce?retryWrites=true&w=majority&appName=Cluster0

# Database name to use
MONGODB_DB=ecommerce

# Other app secrets (optional examples)
API_KEY=admin-secret-key-123
REVALIDATION_SECRET=your-super-secret-key
